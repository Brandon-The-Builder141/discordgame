Drop tabletop GLB or GLTF assets here.

The current board uses composed Three.js miniature meshes so the game remains runnable without downloads. Future production art can replace the procedural hero, road stalker, wagon, terrain, and dice by loading files from this folder through the existing `/assets/...` static path.
